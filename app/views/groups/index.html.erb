<h1>Groups Index</h1>
<% @groups.each do |group| %>
  <a href="<%= group_transacs_path(group) %>">
    Icon: <%= group.icon %>
    Name: <%= group.name %>
    Amount: <%= group.total_amount %>
  </a>
<% end %>

<%= link_to "Add a new category", new_group_path %>
